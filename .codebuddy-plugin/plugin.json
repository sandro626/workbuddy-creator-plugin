{
  "$schema": "https://json.schemastore.org/claude-plugin.json",
  "name": "creator-master",
  "version": "1.0.0",
  "description": "在 WorkBuddy 里用 creator-master 的引擎创作教学应用（绘画 / 课件 / 音乐 / 视频），创作走原平台扣分逻辑、密钥绑个人账号。",
  "description_en": "Create teaching apps (painting / courseware / music / video) on creator-master from inside WorkBuddy. Creation charges points via the platform; keys are user-bound.",
  "author": {
    "name": "creator-master",
    "url": "https://github.com/your-org/creator-master"
  },
  "license": "MIT",
  "keywords": ["education", "k12", "teaching", "mcp", "app-builder", "courseware"],
  "mcpServers": "./.mcp.json",
  "userConfig": {
    "API_ENDPOINT": {
      "type": "string",
      "title": "creator-master 后端地址 (API Endpoint)",
      "description": "creator-master 后端 API 根地址，如 http://localhost:3000 或你的线上地址（不含 /api/v1）。",
      "default": "http://localhost:3000",
      "required": true
    },
    "API_TOKEN": {
      "type": "string",
      "title": "个人 API 密钥 (API Token)",
      "description": "在 creator-master 用户中心「API 密钥」生成的个人密钥（ak_live_...）。仅本地保存，用于鉴权与扣分。",
      "required": true,
      "sensitive": true
    }
  }
}
