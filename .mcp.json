{
  "mcpServers": {
    "creator-master": {
      "command": "${user_config.NODE_BIN}",
      "args": ["${CODEBUDDY_PLUGIN_ROOT}/mcp-server/index.js"],
      "env": {
        "API_ENDPOINT": "${user_config.API_ENDPOINT}",
        "API_TOKEN": "${user_config.API_TOKEN}"
      }
    }
  }
}
